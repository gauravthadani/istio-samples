apiVersion: config.istio.io/v1alpha2
kind: RouteRule
metadata:
  name: sample-retry
  labels:
   version: v1
spec:
  destination:
    name: sample-svc
  route:
  httpReqRetries:
    simpleRetry:
      attempts: 2
